{
  "name": "rmbtsdonations",
  "version": "1.0.0",
  "private": true,
  "description": "RMbtsdonations fan donation site with Vercel APIs for admin settings and proof uploads.",
  "scripts": {
    "preview": "python3 -m http.server 5173",
    "check": "node --check script.js && node --check admin.js && node --check api/config.js && node --check api/proofs.js && node --check api/health.js && node --check api/_defaults.js && node --check api/_http.js && node --check api/_store.js"
  },
  "devDependencies": {
    "vercel": "^54.13.0"
  }
}
